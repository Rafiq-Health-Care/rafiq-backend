spring:
  config:
    import:  optional:file:.env.prod[.properties]
  devtools:
    livereload:
      enabled: false
    restart:
      enabled: false
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: WARN
